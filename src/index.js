#!/usr/bin/env node

`use strict` ;

import React from `react` ;

import ReactDOM from `react-dom` ;

import App from `./assets/main.css` ;

import App from `./App` ;

ReactDOM . render ( <React.StrictMode><App /></React.StrictMode> , document . getElementById ( `root` ) ) ;
